"""
年段一分三率计算系统 - 主程序入口

此文件作为系统的启动点，负责初始化PyQt应用程序并启动GradeAnalyzerApp主窗口。

系统功能：
1. 导入Excel成绩文件
2. 计算学生总分、排名和进退步
3. 统计班级一分三率（平均分、及格率、优秀率）
4. 生成单科班级分析和排名
5. 导入教师信息，支持自动识别班级格式
6. 导出格式化的Excel结果报表，包含染色功能

使用说明：
1. 启动系统后，点击"导入成绩"按钮选择Excel成绩文件
2. 在"科目设置"中配置科目、满分、及格线和优秀线
3. 在"教师设置"中导入或手动输入教师信息
4. 点击"应用参数并计算"按钮开始计算
5. 在各标签页查看计算结果
6. 点击"导出Excel"按钮导出结果报表

版本：0.6
作者：beiyan124与Trae,deepseek
"""

"""
帮助文档

一、系统简介
年段一分三率计算系统是一个专门用于教育教学成绩分析的工具，能够快速计算学生成绩、排名以及班级的各项统计指标。

二、主要功能
1. 成绩导入：支持导入Excel格式的成绩文件
2. 数据计算：计算学生总分、年级排名、班级排名和进退步
3. 班级统计：统计班级的平均分、及格率和优秀率
4. 单科分析：生成各学科的班级分析和排名
5. 教师管理：导入和管理教师信息，支持自动识别班级格式
6. 报表导出：导出格式化的Excel报表，包含染色功能

三、使用步骤
1. 启动系统
2. 导入成绩文件
3. 配置科目和阈值
4. 导入教师信息
5. 执行计算
6. 查看结果
7. 导出报表

四、注意事项
1. 成绩文件必须包含"班级"和"姓名"列
2. 各科目列名应与科目设置中的名称一致
3. 教师信息文件应包含"班级"、"班主任"和各科目教师列
4. 导出的Excel文件可能需要启用编辑才能查看完整格式

五、常见问题
1. 导入失败：检查文件格式是否正确，是否包含必要的列
2. 计算错误：检查科目设置是否正确，是否有缺失数据
3. 导出失败：检查文件路径是否存在，是否有写入权限

六、版本历史
0.6：更新配色方案，优化输出表格的染色机制
0.5：整合所有功能，添加染色功能和教师信息自动识别
"""

"""
使用说明

1. 系统启动
   - 双击main.py文件或在命令行中运行"python main.py"
   - 等待系统加载完成，进入主界面

2. 导入成绩
   - 点击"导入成绩"按钮
   - 选择包含成绩数据的Excel文件
   - 等待系统加载数据完成

3. 科目设置
   - 在"科目设置"标签页中，点击"从数据识别科目"按钮自动识别科目
   - 手动调整科目名称、满分、及格线和优秀线
   - 点击"应用参数"按钮保存设置

4. 教师设置
   - 在"教师设置"标签页中，点击"导入教师"按钮导入教师信息
   - 或手动在表格中输入教师信息
   - 点击"保存教师配置"按钮保存设置

5. 执行计算
   - 点击"应用参数并计算"按钮
   - 等待计算完成，系统会自动更新各标签页的数据

6. 查看结果
   - 在"学生排名"标签页查看学生的总分、排名和进退步
   - 在"班级汇总"标签页查看班级的一分三率统计
   - 在"单科分析"标签页查看各学科的班级分析
   - 在"单科排名"标签页查看各学科的班级排名

7. 导出报表
   - 点击"导出Excel"按钮
   - 选择保存路径和文件名
   - 等待导出完成，系统会提示导出成功

8. 其他功能
   - "清除内存数据"按钮：清除所有已加载的数据，重置系统状态
   - "班级详情"下拉框：选择特定班级查看详细成绩
   - "排名科目"下拉框：选择特定科目查看排名

注意：系统会在导出Excel报表时自动应用染色功能，使成绩分布更加直观。
"""

# 导入必要的模块
import sys  # 导入sys模块，用于处理命令行参数和程序退出
from PyQt5.QtWidgets import QApplication, QStyleFactory
from gui import GradeAnalyzerApp  # 导入PyQt版本的GradeAnalyzerApp类


def main():
    """
    初始化应用程序并启动主窗口
    
    此函数执行以下操作：
    1. 创建QApplication应用程序实例
    2. 设置应用程序样式为Fusion（现代风格）
    3. 创建GradeAnalyzerApp主窗口实例
    4. 显示主窗口
    5. 进入应用程序主事件循环，等待用户交互
    """
    try:
        # 创建QApplication应用程序实例
        # sys.argv包含命令行参数列表
        app = QApplication(sys.argv)
        
        # 设置应用程序样式为Fusion，提供现代、跨平台的外观
        app.setStyle(QStyleFactory.create('Fusion'))
        
        # 创建GradeAnalyzerApp主窗口实例
        # PyQt版本的应用程序不需要传递根窗口参数
        window = GradeAnalyzerApp()
        
        # 显示主窗口
        window.show()
        
        # 进入应用程序主事件循环
        # 此函数会阻塞，直到用户关闭窗口
        # sys.exit()确保程序以正确的退出代码结束
        sys.exit(app.exec_())
    except ImportError as e:
        print(f"导入错误: {str(e)}")
        print("请确保已安装所有必要的依赖包，如PyQt5、pandas、openpyxl等")
        input("按回车键退出...")
    except Exception as e:
        print(f"应用程序启动失败: {str(e)}")
        input("按回车键退出...")


if __name__ == "__main__":
    """
    程序入口点检查
    
    当此文件被直接执行时（而不是被导入为模块），
    条件为真，执行main()函数启动应用程序。
    """
    # 调用main函数，启动年段一分三率计算系统
    main()